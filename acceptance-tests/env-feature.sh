#!/usr/bin/env bash

# Override the version with the branch name
export CA_VERSION=$(cat VERSION-computed)

export AP_VERSION=v0.13.0-feature_BBB-777-la-notify-app-submission
export MG_VERSION=v0.9.0-feature_BBB-777-la-notify
