blue-badge:
  auth-server:
    url: https://authorisation-service.${bb_env}.${url}/bb_auth
  referencedataservice:
    servicehost:
      host: reference-service.${bb_env}.${url}
      port: 443
      scheme: https
  applicationmanagementservice:
    servicehost:
      host: application-service.${bb_env}.${url}
      port: 443
      scheme: https
  paymentservice:
    servicehost:
      host: payment-service.${bb_env}.${url}
      port: 443
      scheme: https
  cryptoservice:
    servicehost:
      host: crypto-service.${bb_env}.${url}
      port: 443
      scheme: https
  messageservice:
    servicehost:
      host: api.${bb_env}.${url}
      port: 443
      scheme: https
      contextpath: /

management:
  server:
    port: ${bb_health:-8081}

server:
  port: ${bb_port:-8080}

# Google Analytics Code
GACode:  ${GACODE:UA-124760983-5}

spring:
  main:
    banner-mode: "off"
  redis:
    host:
    ssl: true
    cluster:
      nodes: ${REDIS_HOSTNAME}:6379
  resources:
    static-locations: classpath:/static/
    cache:
      cachecontrol:
        no-store: false
        max-age: 315360000

amazon:
  s3bucket: ${bb_application_tmp_bucket}
